# 张国林工作记录

## 一、项目概述

本项目为外卖点餐系统后端开发，主要负责商家管理模块的测试工作。商家管理模块是系统的核心功能之一，包含商家的创建、查询、更新、删除以及相关业务功能的测试。

## 二、工作内容

### 2.1 测试用例设计与编写

根据商家管理模块的代码，完成了30个测试用例的设计，覆盖以下测试类型：

#### 2.1.1 功能测试（20个用例）
| 序号 | 测试用例编号 | 测试场景 | 预期结果 |
|------|------------|---------|---------|
| 1 | TC-MERCHANT-001 | 创建商家-正常情况 | 创建成功，返回商家信息 |
| 2 | TC-MERCHANT-002 | 创建商家-缺少必填字段 | 返回错误提示 |
| 3 | TC-MERCHANT-003 | 创建商家-电话号码格式错误 | 返回格式错误提示 |
| 4 | TC-MERCHANT-004 | 获取所有商家 | 返回商家列表 |
| 5 | TC-MERCHANT-005 | 获取指定商家-商家存在 | 返回商家详细信息 |
| 6 | TC-MERCHANT-006 | 获取指定商家-商家不存在 | 返回404错误 |
| 7 | TC-MERCHANT-007 | 获取商家菜品列表-正常情况 | 返回菜品列表 |
| 8 | TC-MERCHANT-008 | 获取商家菜品列表-商家无菜品 | 返回空列表 |
| 9 | TC-MERCHANT-009 | 获取商家今日订单数-正常情况 | 返回订单数量 |
| 10 | TC-MERCHANT-010 | 更新商家-正常情况 | 更新成功 |
| 11 | TC-MERCHANT-011 | 更新商家-商家不存在 | 返回404错误 |
| 12 | TC-MERCHANT-012 | 更新商家-无权限 | 返回403错误 |
| 13 | TC-MERCHANT-013 | 删除商家-正常情况 | 删除成功 |
| 14 | TC-MERCHANT-014 | 删除商家-商家不存在 | 返回404错误 |
| 15 | TC-MERCHANT-015 | 删除商家-有关联菜品 | 删除成功，关联数据一并删除 |
| 16 | TC-MERCHANT-016 | 删除商家-有关联订单 | 删除成功，关联数据一并删除 |
| 17 | TC-MERCHANT-017 | 创建商家-重复user_id | 返回错误提示 |
| 18 | TC-MERCHANT-018 | 更新商家-修改电话号码 | 更新成功 |
| 19 | TC-MERCHANT-019 | 获取商家-分页查询 | 返回分页结果 |
| 20 | TC-MERCHANT-020 | 批量删除商家 | 删除成功 |

#### 2.1.2 参数校验测试（5个用例）
| 序号 | 测试用例编号 | 测试场景 | 预期结果 |
|------|------------|---------|---------|
| 21 | TC-MERCHANT-021 | 电话号码为空 | 返回"联系电话不能为空" |
| 22 | TC-MERCHANT-022 | 电话号码非11位 | 返回"联系电话必须为11位" |
| 23 | TC-MERCHANT-023 | 电话号码包含非数字 | 返回"联系电话只能包含数字" |
| 24 | TC-MERCHANT-024 | 商家名称为空 | 返回"商家名称不能为空" |
| 25 | TC-MERCHANT-025 | 地址为空 | 返回"地址不能为空" |

#### 2.1.3 性能测试（5个用例）
| 序号 | 测试用例编号 | 测试场景 | 预期结果 |
|------|------------|---------|---------|
| 26 | TC-MERCHANT-026 | 接口响应时间统计 | 各接口响应时间<500ms |
| 27 | TC-MERCHANT-027 | 创建商家并发测试 | 10并发下正常响应 |
| 28 | TC-MERCHANT-028 | 查询商家并发测试 | 50并发下正常响应 |
| 29 | TC-MERCHANT-029 | 删除商家并发测试 | 10并发下正常响应 |
| 30 | TC-MERCHANT-030 | 压力测试 | 100并发下系统稳定 |

### 2.2 问题排查与修复

#### 2.2.1 500错误排查

**问题描述：** 调用创建商家接口 `/merchants` 时返回500错误

**排查过程：**
1. 检查服务器日志，定位错误原因
2. 分析 `MerchantController` 和 `MerchantServiceImpl` 代码
3. 检查 `Merchant` 实体类和数据库表结构
4. 定位问题：`toEntity` 方法中未正确设置 `description` 和 `user` 字段

**解决方案：**
1. 修改 `MerchantServiceImpl.toEntity()` 方法
2. 添加 `description` 字段设置
3. 确保 `user` 字段正确关联

#### 2.2.2 循环引用问题

**问题描述：** 获取商家列表时出现JSON序列化失败

**问题原因：** `Merchant` 和 `User` 实体之间存在双向关联，导致循环引用

**解决方案：**
1. 创建 `MerchantListDTO` 数据传输对象
2. 返回 `userId` 而不是完整的 `User` 对象
3. 修改 `findAll()` 方法返回 `List<MerchantListDTO>`

#### 2.2.3 唯一约束冲突

**问题描述：** 重复调用创建商家接口时报错

**问题原因：** `user_id` 字段有唯一约束，一个用户只能拥有一个商家

**解决方案：**
1. 使用未使用的 `user_id` 值（如5或更大）
2. 在测试数据中确保 `user_id` 唯一

#### 2.2.4 删除商家外键约束问题

**问题描述：** 删除商家时因外键约束失败

**问题原因：** 数据库中存在 `orders` 和 `order_details` 表的外键约束

**解决方案：**
1. 在 `OrderRepository` 中添加 `deleteByMerchantId()` 方法
2. 在 `OrderDetailRepository` 中添加 `deleteByOrder_MerchantId()` 方法
3. 修改 `MerchantServiceImpl.deleteMerchant()` 删除顺序：
   - 先删除 `order_details`
   - 再删除 `dishes`
   - 再删除 `orders`
   - 最后删除 `merchant`

#### 2.2.5 403权限错误

**问题描述：** 测试更新商家接口时返回403错误

**问题原因：** `MerchantController.updateMerchant()` 方法中有权限校验，要求当前用户具有MERCHANT角色

**解决方案：**
1. 测试时使用具有商家角色的用户
2. 或在测试环境中调整权限校验逻辑

### 2.3 数据准备

#### 2.3.1 今日订单数据生成

为商家ID为1生成今日订单数据，执行以下SQL脚本：

```sql
-- 插入今日订单
INSERT INTO orders (user_id, merchant_id, status, order_time, total_amount, address, phone, recipient_name)
VALUES (1, 1, 'COMPLETED', NOW(), 58.00, '北京市海淀区中关村大街1号', '13800138000', '张三');

-- 获取刚插入的订单ID
SET @order_id = LAST_INSERT_ID();

-- 插入订单详情
INSERT INTO order_details (order_id, dish_id, dish_name, quantity, unit_price)
VALUES (@order_id, 1, '宫保鸡丁', 1, 28.00);

INSERT INTO order_details (order_id, dish_id, dish_name, quantity, unit_price)
VALUES (@order_id, 2, '鱼香肉丝', 1, 30.00);
```

### 2.4 验证功能实现

#### 2.4.1 电话号码格式验证

实现创建商家时的电话号码格式校验：

1. 在 `MerchantDTO` 中添加验证注解：
   ```java
   @NotBlank(message = "联系电话不能为空")
   @Pattern(regexp = "^[0-9]+$", message = "联系电话只能包含数字")
   @Size(min = 11, max = 11, message = "联系电话必须为11位")
   private String phone;
   ```

2. 在 `MerchantController` 中启用验证：
   ```java
   @PostMapping
   public R<MerchantDTO> createMerchant(@Valid @RequestBody MerchantDTO merchantDTO) {
       return R.ok(merchantService.createMerchant(merchantDTO));
   }
   ```

3. 创建全局异常处理器 `GlobalExceptionHandler`：
   ```java
   @RestControllerAdvice
   public class GlobalExceptionHandler {
       @ExceptionHandler(MethodArgumentNotValidException.class)
       @ResponseStatus(HttpStatus.BAD_REQUEST)
       public R<Map<String, String>> handleValidationExceptions(MethodArgumentNotValidException ex) {
           // 处理验证异常，返回400状态码和错误信息
       }
   }
   ```

4. 在 `pom.xml` 中添加验证依赖：
   ```xml
   <dependency>
       <groupId>org.springframework.boot</groupId>
       <artifactId>spring-boot-starter-validation</artifactId>
   </dependency>
   ```

## 三、技术要点

### 3.1 Spring Boot开发
- Controller-Service-Repository架构
- RESTful API设计
- 依赖注入（ Lombok @RequiredArgsConstructor）

### 3.2 JPA数据访问
- Entity-DTO转换
- 数据库外键约束处理
- 级联删除实现

### 3.3 数据验证
- Jakarta Bean Validation
- 自定义验证消息
- 全局异常处理

### 3.4 性能优化
- DTO避免循环引用
- 批量操作优化
- 并发访问处理

## 四、总结

本次工作完成了商家管理模块的全面测试，包括功能测试、参数校验测试和性能测试。在测试过程中发现并修复了多个问题，包括：
- 实体转换字段缺失
- 循环引用导致序列化失败
- 数据库外键约束冲突
- 权限校验问题
- 参数验证功能完善

通过本次工作，确保了商家管理模块的稳定性和可靠性，为系统的上线奠定了基础。

---

**记录人：** 张国林

**记录日期：** 2025-12-29
