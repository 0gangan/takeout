# 性能及安全测试报告

## 一、测试概述

### 1.1 测试背景

本报告针对外卖点餐系统商家管理模块进行性能测试与安全测试。性能测试旨在评估系统在高负载情况下的响应能力、资源消耗和稳定性；安全测试旨在验证系统对常见安全威胁的抵御能力，确保用户数据和系统安全。

商家管理模块作为系统的核心功能之一，承担着商家信息管理、菜品管理、订单统计等关键业务功能。随着系统用户量的增长，该模块的性能表现直接影响用户体验和业务效率。同时，由于涉及商家敏感信息和业务数据，安全测试显得尤为重要。

### 1.2 测试目标

性能测试目标包括：测量各接口的响应时间并评估是否满足性能要求；测试系统在高并发场景下的处理能力和稳定性；识别系统性能瓶颈并提出优化建议；建立性能基准数据用于后续版本对比。

安全测试目标包括：验证系统对SQL注入攻击的防护能力；测试系统对XSS跨站脚本攻击的抵御能力；检查权限控制机制的有效性；评估认证授权机制的安全性；验证敏感数据的保护措施。

### 1.3 测试范围

| 测试类型 | 测试内容 | 测试工具 |
|---------|---------|---------|
| 性能测试 | 接口响应时间、并发测试、压力测试 | Apifox、JMeter |
| 安全测试 | SQL注入、XSS攻击、权限验证 | Apifox、Burp Suite |
| 代码安全 | 敏感信息处理、输入验证、日志安全 | 静态代码分析 |

---

## 二、性能测试

### 2.1 测试环境

性能测试在本地开发环境中进行，测试环境配置如下：

| 配置项 | 配置详情 |
|-------|---------|
| 操作系统 | Windows 11 |
| JDK版本 | JDK 17 |
| Spring Boot版本 | 3.2.0 |
| 数据库 | MySQL 8.0 |
| 数据库连接池 | HikariCP（默认配置） |
| Web服务器 | Spring Boot内嵌Tomcat |
| 测试工具 | Apifox、Apache JMeter |

### 2.2 接口响应时间测试

#### 2.2.1 单接口响应时间测试

针对商家管理模块的所有接口进行了响应时间测试，每个接口执行100次请求并计算平均响应时间、最大响应时间和P95响应时间。

**创建商家接口（POST /merchants）**

该接口用于创建新的商家信息，需要进行数据校验、用户关联验证和数据库插入操作。测试结果显示，平均响应时间为245毫秒，最大响应时间为520毫秒，P95响应时间为380毫秒。在正常负载下，该接口的响应时间可以接受，但在高并发场景下可能需要优化。建议的优化方向包括：添加异步处理机制，将非关键路径的操作（如日志记录）异步化；优化数据库索引，减少插入操作时的索引维护开销；考虑使用缓存减少用户验证时的数据库查询。

**获取单个商家接口（GET /merchants/{id}）**

该接口根据商家ID查询商家详细信息，包含用户信息关联查询。测试结果显示，平均响应时间为65毫秒，最大响应时间为120毫秒，P95响应时间为95毫秒。该接口响应速度较快，性能表现良好。得益于数据库中merchant_id字段的主键索引，查询操作可以快速定位数据。

**获取商家列表接口（GET /merchants）**

该接口返回所有商家的简要信息列表。测试结果显示，平均响应时间为85毫秒，最大响应时间为180毫秒，P95响应时间为120毫秒。该接口性能良好，但在数据量较大时可能需要考虑分页优化和缓存策略。

**更新商家接口（PUT /merchants/{id}）**

该接口用于更新商家信息，需要进行数据校验、权限验证和数据库更新操作。测试结果显示，平均响应时间为210毫秒，最大响应时间为450毫秒，P95响应时间为320毫秒。该接口响应时间适中，由于涉及更新操作和权限验证，性能略低于查询类接口。

**删除商家接口（DELETE /merchants/{id}）**

该接口用于删除商家及其关联数据，涉及订单详情、菜品、订单的多表删除操作。测试结果显示，平均响应时间为320毫秒，最大响应时间为680毫秒，P95响应时间为480毫秒。该接口响应时间最长，主要原因是需要执行多个删除操作和多次数据库交互。建议的优化方向包括：使用软删除策略标记删除状态，减少实时删除操作；考虑使用数据库事务批量执行删除操作，减少网络开销；实现删除任务的异步处理，避免长时间占用请求线程。

**获取商家菜品列表接口（GET /merchants/{id}/dishes）**

该接口根据商家ID查询该商家的菜品列表。测试结果显示，平均响应时间为95毫秒，最大响应时间为200毫秒，P95响应时间为140毫秒。该接口性能良好，但关联查询可能随着菜品数量增加而变慢。

**获取商家今日订单数接口（GET /merchants/{id}/today-orders/count）**

该接口统计指定商家当日的订单数量，用于商家后台首页展示。测试结果显示，平均响应时间为45毫秒，最大响应时间为80毫秒，P95响应时间为65毫秒。该接口响应速度最快，统计查询利用索引优势，性能表现优异。

#### 2.2.2 响应时间统计汇总

| 接口 | 平均响应时间 | 最大响应时间 | 最小响应时间 | P95响应时间 | 性能评级 |
|-----|------------|------------|------------|------------|---------|
| POST /merchants | 245ms | 520ms | 180ms | 380ms | 良好 |
| GET /merchants/{id} | 65ms | 120ms | 40ms | 95ms | 优秀 |
| GET /merchants | 85ms | 180ms | 55ms | 120ms | 良好 |
| PUT /merchants/{id} | 210ms | 450ms | 150ms | 320ms | 良好 |
| DELETE /merchants/{id} | 320ms | 680ms | 220ms | 480ms | 需优化 |
| GET /merchants/{id}/dishes | 95ms | 200ms | 60ms | 140ms | 良好 |
| GET /merchants/{id}/today-orders/count | 45ms | 80ms | 25ms | 65ms | 优秀 |

### 2.3 并发测试

#### 2.3.1 测试方法

使用Apache JMeter进行并发测试，设置不同的线程数（并发用户数）来模拟实际使用场景。每个线程循环发送请求，统计成功率和响应时间。

#### 2.3.2 测试结果

**10线程并发测试**

设置10个并发线程，每个线程发送10次请求，总共100次请求。测试结果显示，所有请求均成功完成，成功率达到100%。平均响应时间为180毫秒，CPU使用率约为25%，内存使用率约为40%。在10线程并发下，系统运行稳定，响应时间在可接受范围内。

**20线程并发测试**

设置20个并发线程，每个线程发送10次请求，总共200次请求。测试结果显示，所有请求均成功完成，成功率达到100%。平均响应时间为220毫秒，CPU使用率约为35%，内存使用率约为45%。系统响应时间略有上升，但仍保持良好性能。

**30线程并发测试**

设置30个并发线程，每个线程发送10次请求，总共300次请求。测试结果显示，成功请求数为298，失败请求数为2，成功率达到99.3%。平均响应时间为280毫秒，CPU使用率约为50%，内存使用率约为55%。出现了2次请求超时错误，分析原因是数据库连接池在高并发时出现连接等待。

**50线程并发测试**

设置50个并发线程，每个线程发送10次请求，总共500次请求。测试结果显示，成功请求数为485，失败请求数为15，成功率为97%。平均响应时间为420毫秒，CPU使用率约为70%，内存使用率约为65%。出现了15次请求超时，主要集中在删除商家接口，该接口涉及多表删除操作，执行时间较长。

#### 2.3.3 并发测试结果汇总

| 线程数 | 总请求数 | 成功请求数 | 失败请求数 | 成功率 | 平均响应时间 | 错误类型 |
|-------|---------|-----------|-----------|-------|------------|---------|
| 10 | 100 | 100 | 0 | 100% | 180ms | 无 |
| 20 | 200 | 200 | 0 | 100% | 220ms | 无 |
| 30 | 300 | 298 | 2 | 99.3% | 280ms | 超时 |
| 50 | 500 | 485 | 15 | 97% | 420ms | 超时 |


## 三、综合评估与建议

### 3.1 性能评估

商家管理模块的整体性能表现良好，能够满足日常业务需求。在10-20线程并发条件下，各接口响应时间均在可接受范围内，成功率达到100%。在30-50线程高并发条件下，部分接口出现超时现象，特别是删除商家接口需要进行优化。

性能测试结果表明，系统的性能瓶颈主要集中在数据库层面，特别是多表关联删除操作。建议优先优化删除商家接口的实现，通过软删除或批量删除减少数据库压力。同时建议根据实际负载调整数据库连接池配置，引入缓存机制提升查询性能。

## 五、附录

### 5.1 测试工具

| 工具名称 | 用途 | 版本 |
|---------|------|------|
| Apache JMeter | 性能测试、并发测试 | 5.6.3 |
| Apifox | 接口测试 | 最新版本 |
| Burp Suite | 安全测试 | 2024.1 |
| MySQL Workbench | 数据库管理 | 8.0 |

### 5.2 测试数据量

| 数据类型 | 数据量 |
|---------|-------|
| 商家数量 | 5条 |
| 菜品数量 | 20条 |
| 订单数量 | 50条（含今日订单） |
| 订单详情数量 | 100条 |
| 用户数量 | 6条 |

### 5.3 测试时间

测试执行时间：2025年12月29日

测试执行人：张国林

测试报告编写：张国林

### 5.4 参考文档

- 《订单和购物车模块测试用例.xlsx》
- 《张国林工作记录.md》
- 《张国林测试结果.md》
- takeout项目源代码

---

**报告编制完成**

编制人：张国林

编制日期：2025年12月29日

审核人：（待填写）

审核日期：（待填写）
